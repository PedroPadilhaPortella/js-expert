{
  "name": "observer",
  "version": "1.0.0",
  "description": "",
  "main": "jest.config.js",
  "type": "module",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "start": "node ./src/index.js",
    "test": "SET NODE_OPTIONS=--experimental-vm-modules && npx jest --runInBand --silent --watch --config ./jest.config.js",
    "test:coverage": "SET NODE_OPTIONS=--experimental-vm-modules && npx jest --silent --config ./jest.config.js --coverage"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^29.0.3"
  }
}
